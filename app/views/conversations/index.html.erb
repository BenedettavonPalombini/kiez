<h1 class="d-flex justify-content-center"> Inbox </h1>

<% conversations = Conversation.all %>
<% conversations.each do |conversation| %>
<div class= "card">
        <div class= "bcard card-body ">
          <div class="row align-items-start">
            <div class="col">
            </div>
            <div class="col">
              <h2> <%= link_to conversation.other_users(current_user).first.first_name, conversation_path(conversation) %> </h2>
            </div>
<% end %>
