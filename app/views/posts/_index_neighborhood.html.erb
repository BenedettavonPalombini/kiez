<div class="container bg-card text-center mt-5">
  <ul>
      <% @posts.each do |post| %>
        <%# < photo > %>
        <li><%= post.title %></li>
        <li><%= post.content %></li>
        <li><%= post.category %></li>
        <li><%= post.content %></li>
        <li><%= post.content %></li>
      <% end %>
  </ul>
</div>
